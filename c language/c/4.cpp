#include<stdio.h>
int main()
{
	float length,breadth,area;
	printf("enter a number",length);
	scanf("%f",&length);
	printf("\n enter a number",breadth);
	scanf("%f",&breadth);
	area=length*breadth;
	printf("area of the rectangle=%f",area);
	return 0;
}
